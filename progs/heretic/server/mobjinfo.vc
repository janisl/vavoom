//**************************************************************************
//**
//**	##   ##    ##    ##   ##   ####     ####   ###     ###
//**	##   ##  ##  ##  ##   ##  ##  ##   ##  ##  ####   ####
//**	 ## ##  ##    ##  ## ##  ##    ## ##    ## ## ## ## ##
//**	 ## ##  ########  ## ##  ##    ## ##    ## ##  ###  ##
//**	  ###   ##    ##   ###    ##  ##   ##  ##  ##       ##
//**	   #    ##    ##    #      ####     ####   ##       ##
//**
//**	$Id$
//**
//**	Copyright (C) 1999-2001 JÆnis Legzdi·ý
//**
//**	This program is free software; you can redistribute it and/or
//**  modify it under the terms of the GNU General Public License
//**  as published by the Free Software Foundation; either version 2
//**  of the License, or (at your option) any later version.
//**
//**	This program is distributed in the hope that it will be useful,
//**  but WITHOUT ANY WARRANTY; without even the implied warranty of
//**  MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
//**  GNU General Public License for more details.
//**
//**************************************************************************
//**
//**	Mobj info table
//**
//**	Generated by makeinfo.
//**
//**************************************************************************

void generic_mobj_start(mobj_t *self, mthing_t *mthing);

//==========================================================================
//
//	misc_MISC0
//
//==========================================================================

void misc_MISC0(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ITEM_PTN1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_ITEMSHIELD1
//
//==========================================================================

void misc_ITEMSHIELD1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ITEM_SHLD1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_ITEMSHIELD2
//
//==========================================================================

void misc_ITEMSHIELD2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ITEM_SHD2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC1
//
//==========================================================================

void misc_MISC1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ITEM_BAGH1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC2
//
//==========================================================================

void misc_MISC2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ITEM_SPMP1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_ARTIINVISIBILITY
//
//==========================================================================

void misc_ARTIINVISIBILITY(mobj_t *self, mthing_t *mthing)
{
	self->translucency = 33;
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_INVS1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC3
//
//==========================================================================

void misc_MISC3(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_PTN2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_ARTIFLY
//
//==========================================================================

void misc_ARTIFLY(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_SOAR1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_ARTIINVULNERABILITY
//
//==========================================================================

void misc_ARTIINVULNERABILITY(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_INVU1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_ARTITOMEOFPOWER
//
//==========================================================================

void misc_ARTITOMEOFPOWER(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_PWBK1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_ARTIEGG
//
//==========================================================================

void misc_ARTIEGG(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_EGGC1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_EGGFX
//
//==========================================================================

void misc_EGGFX(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 630.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 1;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_EGGFX1;
	self->deathstate = S_EGGFXI1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_ARTISUPERHEAL
//
//==========================================================================

void misc_ARTISUPERHEAL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_SPHL1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC4
//
//==========================================================================

void misc_MISC4(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_TRCH1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC5
//
//==========================================================================

void misc_MISC5(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_FBMB1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_FIREBOMB
//
//==========================================================================

void misc_FIREBOMB(mobj_t *self, mthing_t *mthing)
{
	self->translucency = 33;
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOGRAVITY;
	self->spawnstate = S_FIREBOMB1;
	self->sound_death = GetSoundID("PhoenixHit");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_ARTITELEPORT
//
//==========================================================================

void misc_ARTITELEPORT(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL|MF_COUNTITEM;
	self->flags2 = MF2_FLOATBOB;
	self->spawnstate = S_ARTI_ATLP1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_POD
//
//==========================================================================

void misc_POD(mobj_t *self, mthing_t *mthing)
{
	self->health = 45;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 54.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 255;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_DROPOFF|MF_NOBLOOD;
	self->flags2 = MF2_WINDTHRUST|MF2_PUSHABLE|MF2_SLIDE|MF2_PASSMOBJ|MF2_TELESTOMP;
	self->spawnstate = S_POD_WAIT1;
	self->painstate = S_POD_PAIN1;
	self->deathstate = S_POD_DIE1;
	self->sound_death = GetSoundID("PodExplode");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_PODGOO
//
//==========================================================================

void misc_PODGOO(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 2.0;
	self->height = 4.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_LOGRAV|MF2_NOTELEPORT|MF2_CANNOTPUSH;
	self->spawnstate = S_PODGOO1;
	self->deathstate = S_PODGOOX;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_PODGENERATOR
//
//==========================================================================

void misc_PODGENERATOR(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOSECTOR|MF_NOBLOCKMAP;
	self->spawnstate = S_PODGENERATOR;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SPLASH
//
//==========================================================================

void misc_SPLASH(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 2.0;
	self->height = 4.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_LOGRAV|MF2_NOTELEPORT|MF2_CANNOTPUSH;
	self->spawnstate = S_SPLASH1;
	self->deathstate = S_SPLASHX;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SPLASHBASE
//
//==========================================================================

void misc_SPLASHBASE(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP;
	self->spawnstate = S_SPLASHBASE1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_LAVASPLASH
//
//==========================================================================

void misc_LAVASPLASH(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP;
	self->spawnstate = S_LAVASPLASH1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_LAVASMOKE
//
//==========================================================================

void misc_LAVASMOKE(mobj_t *self, mthing_t *mthing)
{
	self->translucency = 33;
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_LAVASMOKE1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SLUDGECHUNK
//
//==========================================================================

void misc_SLUDGECHUNK(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 2.0;
	self->height = 4.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_LOGRAV|MF2_NOTELEPORT|MF2_CANNOTPUSH;
	self->spawnstate = S_SLUDGECHUNK1;
	self->deathstate = S_SLUDGECHUNKX;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SLUDGESPLASH
//
//==========================================================================

void misc_SLUDGESPLASH(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP;
	self->spawnstate = S_SLUDGESPLASH1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SKULLHANG70
//
//==========================================================================

void misc_SKULLHANG70(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 70.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_SKULLHANG70_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SKULLHANG60
//
//==========================================================================

void misc_SKULLHANG60(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 60.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_SKULLHANG60_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SKULLHANG45
//
//==========================================================================

void misc_SKULLHANG45(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 45.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_SKULLHANG45_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SKULLHANG35
//
//==========================================================================

void misc_SKULLHANG35(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 35.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_SKULLHANG35_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_CHANDELIER
//
//==========================================================================

void misc_CHANDELIER(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 60.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_CHANDELIER1;
	generic_mobj_start(self, mthing);
	AddStaticLight(self->origin,
		mthing->arg1 ? itof(mthing->arg1) * 8.0 : 200.0);
}

//==========================================================================
//
//	misc_SERPTORCH
//
//==========================================================================

void misc_SERPTORCH(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 54.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_SERPTORCH1;
	generic_mobj_start(self, mthing);
	AddStaticLight(self->origin + vector(0.0, 0.0, 32.0),
		mthing->arg1 ? itof(mthing->arg1) * 8.0 : 128.0);
}

//==========================================================================
//
//	misc_SMALLPILLAR
//
//==========================================================================

void misc_SMALLPILLAR(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 34.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_SMALLPILLAR;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_STALAGMITESMALL
//
//==========================================================================

void misc_STALAGMITESMALL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 32.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_STALAGMITESMALL;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_STALAGMITELARGE
//
//==========================================================================

void misc_STALAGMITELARGE(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 64.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_STALAGMITELARGE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_STALACTITESMALL
//
//==========================================================================

void misc_STALACTITESMALL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 36.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID|MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_STALACTITESMALL;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_STALACTITELARGE
//
//==========================================================================

void misc_STALACTITELARGE(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 68.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID|MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_STALACTITELARGE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC6
//
//==========================================================================

void misc_MISC6(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 44.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_FIREBRAZIER1;
	generic_mobj_start(self, mthing);
	AddStaticLight(self->origin + vector(0.0, 0.0, 32.0),
		mthing->arg1 ? itof(mthing->arg1) * 8.0 : 200.0);
}

//==========================================================================
//
//	misc_BARREL
//
//==========================================================================

void misc_BARREL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 32.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_BARREL;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC7
//
//==========================================================================

void misc_MISC7(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 14.0;
	self->height = 128.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_BRPILLAR;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC8
//
//==========================================================================

void misc_MISC8(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 23.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_MOSS1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC9
//
//==========================================================================

void misc_MISC9(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 27.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_MOSS2;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC10
//
//==========================================================================

void misc_MISC10(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOGRAVITY;
	self->spawnstate = S_WALLTORCH1;
	generic_mobj_start(self, mthing);
	AddStaticLight(self->origin + vector(0.0, 0.0, 80.0),
		mthing->arg1 ? itof(mthing->arg1) * 8.0 : 200.0);
}

//==========================================================================
//
//	misc_MISC11
//
//==========================================================================

void misc_MISC11(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 104.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID|MF_SPAWNCEILING|MF_NOGRAVITY;
	self->spawnstate = S_HANGINGCORPSE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_KEYGIZMOBLUE
//
//==========================================================================

void misc_KEYGIZMOBLUE(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 50.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_KEYGIZMO1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_KEYGIZMOGREEN
//
//==========================================================================

void misc_KEYGIZMOGREEN(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 50.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_KEYGIZMO1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_KEYGIZMOYELLOW
//
//==========================================================================

void misc_KEYGIZMOYELLOW(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 50.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_KEYGIZMO1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_KEYGIZMOFLOAT
//
//==========================================================================

void misc_KEYGIZMOFLOAT(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID|MF_NOGRAVITY;
	self->spawnstate = S_KGZ_START;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC12
//
//==========================================================================

void misc_MISC12(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 20.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SOLID;
	self->spawnstate = S_VOLCANO1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_VOLCANOBLAST
//
//==========================================================================

void misc_VOLCANOBLAST(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 70.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_LOGRAV|MF2_NOTELEPORT|MF2_FIREDAMAGE;
	self->spawnstate = S_VOLCANOBALL1;
	self->deathstate = S_VOLCANOBALLX1;
	self->sound_death = GetSoundID("VolcanoHit");
	self->effects = EF_DL_RED;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_VOLCANOTBLAST
//
//==========================================================================

void misc_VOLCANOTBLAST(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 70.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 1;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_LOGRAV|MF2_NOTELEPORT|MF2_FIREDAMAGE;
	self->spawnstate = S_VOLCANOTBALL1;
	self->deathstate = S_VOLCANOTBALLX1;
	self->effects = EF_DL_RED;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_TELEGLITGEN
//
//==========================================================================

void misc_TELEGLITGEN(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOSECTOR|MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_TELEGLITGEN1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_TELEGLITGEN2
//
//==========================================================================

void misc_TELEGLITGEN2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOSECTOR|MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_TELEGLITGEN2;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_TELEGLITTER
//
//==========================================================================

void misc_TELEGLITTER(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_MISSILE;
	self->spawnstate = S_TELEGLITTER1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_TELEGLITTER2
//
//==========================================================================

void misc_TELEGLITTER2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_MISSILE;
	self->spawnstate = S_TELEGLITTER2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_TFOG
//
//==========================================================================

void misc_TFOG(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_TFOG1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_TELEPORTMAN
//
//==========================================================================

void misc_TELEPORTMAN(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOSECTOR|MF_NOBLOCKMAP;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_STAFFPUFF
//
//==========================================================================

void misc_STAFFPUFF(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_STAFFPUFF1;
	self->sound_attack = GetSoundID("StaffHit");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_STAFFPUFF2
//
//==========================================================================

void misc_STAFFPUFF2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_STAFFPUFF2_1;
	self->sound_attack = GetSoundID("StaffHit2");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BEAKPUFF
//
//==========================================================================

void misc_BEAKPUFF(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_STAFFPUFF1;
	self->sound_attack = GetSoundID("ChickenAttack");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC13
//
//==========================================================================

void misc_MISC13(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_WGNT;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_GAUNTLETPUFF1
//
//==========================================================================

void misc_GAUNTLETPUFF1(mobj_t *self, mthing_t *mthing)
{
	self->translucency = 33;
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_GAUNTLETPUFF1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_GAUNTLETPUFF2
//
//==========================================================================

void misc_GAUNTLETPUFF2(mobj_t *self, mthing_t *mthing)
{
	self->translucency = 33;
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_GAUNTLETPUFF2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC14
//
//==========================================================================

void misc_MISC14(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_BLSR;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BLASTERFX1
//
//==========================================================================

void misc_BLASTERFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 6440.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_BLASTERFX1_1;
	self->deathstate = S_BLASTERFXI1_1;
	self->sound_death = GetSoundID("BlasterHit");
	self->effects = EF_DL_BLUE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BLASTERSMOKE
//
//==========================================================================

void misc_BLASTERSMOKE(mobj_t *self, mthing_t *mthing)
{
	self->translucency = 33;
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->flags2 = MF2_NOTELEPORT|MF2_CANNOTPUSH;
	self->spawnstate = S_BLASTERSMOKE1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_RIPPER
//
//==========================================================================

void misc_RIPPER(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 490.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 1;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT|MF2_RIP;
	self->spawnstate = S_RIPPER1;
	self->deathstate = S_RIPPERX1;
	self->sound_death = GetSoundID("SkullRodHit");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BLASTERPUFF1
//
//==========================================================================

void misc_BLASTERPUFF1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_BLASTERPUFF1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BLASTERPUFF2
//
//==========================================================================

void misc_BLASTERPUFF2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_BLASTERPUFF2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_WMACE
//
//==========================================================================

void misc_WMACE(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_WMCE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MACEFX1
//
//==========================================================================

void misc_MACEFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 700.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_FLOORBOUNCE|MF2_THRUGHOST|MF2_NOTELEPORT;
	self->spawnstate = S_MACEFX1_1;
	self->deathstate = S_MACEFXI1_1;
	self->sound_sight = GetSoundID("MaceFire");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MACEFX2
//
//==========================================================================

void misc_MACEFX2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 6;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_LOGRAV|MF2_FLOORBOUNCE|MF2_THRUGHOST|MF2_NOTELEPORT;
	self->spawnstate = S_MACEFX2_1;
	self->deathstate = S_MACEFXI2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MACEFX3
//
//==========================================================================

void misc_MACEFX3(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 245.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 4;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_LOGRAV|MF2_FLOORBOUNCE|MF2_THRUGHOST|MF2_NOTELEPORT;
	self->spawnstate = S_MACEFX3_1;
	self->deathstate = S_MACEFXI1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MACEFX4
//
//==========================================================================

void misc_MACEFX4(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 245.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 18;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_LOGRAV|MF2_FLOORBOUNCE|MF2_THRUGHOST|MF2_TELESTOMP;
	self->spawnstate = S_MACEFX4_1;
	self->deathstate = S_MACEFXI4_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_WSKULLROD
//
//==========================================================================

void misc_WSKULLROD(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_WSKL;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_HORNRODFX1
//
//==========================================================================

void misc_HORNRODFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 770.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 3;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_WINDTHRUST|MF2_NOTELEPORT;
	self->spawnstate = S_HRODFX1_1;
	self->deathstate = S_HRODFXI1_1;
	self->sound_sight = GetSoundID("SkullRodFire");
	self->sound_death = GetSoundID("SkullRodHit");
	self->effects = EF_DL_RED;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_HORNRODFX2
//
//==========================================================================

void misc_HORNRODFX2(mobj_t *self, mthing_t *mthing)
{
	self->health = 140;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 770.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 10;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_HRODFX2_1;
	self->deathstate = S_HRODFXI2_1;
	self->sound_sight = GetSoundID("SkullRodFire");
	self->sound_death = GetSoundID("SkullRodHit");
	self->effects = EF_DL_RED;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_RAINPLR1
//
//==========================================================================

void misc_RAINPLR1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 5.0;
	self->height = 12.0;
	self->mass = 100.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 5;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_RAINPLR1_1;
	self->deathstate = S_RAINPLR1X_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_RAINPLR2
//
//==========================================================================

void misc_RAINPLR2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 5.0;
	self->height = 12.0;
	self->mass = 100.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 5;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_RAINPLR2_1;
	self->deathstate = S_RAINPLR2X_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_RAINPLR3
//
//==========================================================================

void misc_RAINPLR3(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 5.0;
	self->height = 12.0;
	self->mass = 100.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 5;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_RAINPLR3_1;
	self->deathstate = S_RAINPLR3X_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_RAINPLR4
//
//==========================================================================

void misc_RAINPLR4(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 5.0;
	self->height = 12.0;
	self->mass = 100.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 5;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_RAINPLR4_1;
	self->deathstate = S_RAINPLR4X_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_GOLDWANDFX1
//
//==========================================================================

void misc_GOLDWANDFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 10.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 770.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_GWANDFX1_1;
	self->deathstate = S_GWANDFXI1_1;
	self->sound_death = GetSoundID("GoldWandFire");
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_GOLDWANDFX2
//
//==========================================================================

void misc_GOLDWANDFX2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 10.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 630.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 1;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_GWANDFX2_1;
	self->deathstate = S_GWANDFXI1_1;
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_GOLDWANDPUFF1
//
//==========================================================================

void misc_GOLDWANDPUFF1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_GWANDPUFF1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_GOLDWANDPUFF2
//
//==========================================================================

void misc_GOLDWANDPUFF2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_GWANDFXI1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_WPHOENIXROD
//
//==========================================================================

void misc_WPHOENIXROD(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_WPHX;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_PHOENIXFX1
//
//==========================================================================

void misc_PHOENIXFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 11.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 700.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 20;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_THRUGHOST|MF2_NOTELEPORT;
	self->spawnstate = S_PHOENIXFX1_1;
	self->deathstate = S_PHOENIXFXI1_1;
	self->sound_sight = GetSoundID("PhoenixFire");
	self->sound_death = GetSoundID("PhoenixHit");
	self->effects = EF_DL_RED;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_PHOENIXPUFF
//
//==========================================================================

void misc_PHOENIXPUFF(mobj_t *self, mthing_t *mthing)
{
	self->translucency = 33;
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->flags2 = MF2_NOTELEPORT|MF2_CANNOTPUSH;
	self->spawnstate = S_PHOENIXPUFF1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_PHOENIXFX2
//
//==========================================================================

void misc_PHOENIXFX2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 6.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT|MF2_FIREDAMAGE;
	self->spawnstate = S_PHOENIXFX2_1;
	self->deathstate = S_PHOENIXFXI2_1;
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MISC15
//
//==========================================================================

void misc_MISC15(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_WBOW;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_CRBOWFX1
//
//==========================================================================

void misc_CRBOWFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 11.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 1050.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 10;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_CRBOWFX1;
	self->deathstate = S_CRBOWFXI1_1;
	self->sound_sight = GetSoundID("BowFire");
	self->sound_death = GetSoundID("SkullRodHit");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_CRBOWFX2
//
//==========================================================================

void misc_CRBOWFX2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 11.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 1120.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 6;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_CRBOWFX2;
	self->deathstate = S_CRBOWFXI1_1;
	self->sound_sight = GetSoundID("BowFire");
	self->sound_death = GetSoundID("SkullRodHit");
	self->effects = EF_DL_GREEN;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_CRBOWFX3
//
//==========================================================================

void misc_CRBOWFX3(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 11.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 700.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_WINDTHRUST|MF2_THRUGHOST|MF2_NOTELEPORT;
	self->spawnstate = S_CRBOWFX3;
	self->deathstate = S_CRBOWFXI3_1;
	self->sound_death = GetSoundID("SkullRodHit");
	self->effects = EF_DL_GREEN;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_CRBOWFX4
//
//==========================================================================

void misc_CRBOWFX4(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP;
	self->flags2 = MF2_LOGRAV;
	self->spawnstate = S_CRBOWFX4_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BLOOD
//
//==========================================================================

void misc_BLOOD(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP;
	self->spawnstate = S_BLOOD1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BLOODSPLATTER
//
//==========================================================================

void misc_BLOODSPLATTER(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 2.0;
	self->height = 4.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT|MF2_CANNOTPUSH;
	self->spawnstate = S_BLOODSPLATTER1;
	self->deathstate = S_BLOODSPLATTERX;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_PLAYER
//
//==========================================================================

void misc_PLAYER(mobj_t *self, mthing_t *mthing)
{
	self->health = 100;
	self->spawnhealth = self->health;
	self->gibshealth = -50;
	self->radius = 16.0;
	self->height = 56.0;
	self->mass = 100.0;
	self->painchance = 255;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_DROPOFF|MF_PICKUP;
	self->flags2 = MF2_WINDTHRUST|MF2_FOOTCLIP|MF2_SLIDE|MF2_PASSMOBJ|MF2_TELESTOMP;
	self->spawnstate = S_PLAY;
	self->seestate = S_PLAY_RUN1;
	self->missilestate = S_PLAY_ATK1;
	self->painstate = S_PLAY_PAIN;
	self->deathstate = S_PLAY_DIE1;
	self->xdeathstate = S_PLAY_XDIE1;
	self->sound_pain = GetSoundID("PlayerPain");
	self->sound_death = GetSoundID("PlayerDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BLOODYSKULL
//
//==========================================================================

void misc_BLOODYSKULL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 4.0;
	self->height = 4.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF;
	self->flags2 = MF2_LOGRAV|MF2_CANNOTPUSH;
	self->spawnstate = S_BLOODYSKULL1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_CHICPLAYER
//
//==========================================================================

void misc_CHICPLAYER(mobj_t *self, mthing_t *mthing)
{
	self->health = 100;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 24.0;
	self->mass = 100.0;
	self->painchance = 255;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_DROPOFF;
	self->flags2 = MF2_LOGRAV|MF2_WINDTHRUST|MF2_FOOTCLIP|MF2_SLIDE|MF2_PASSMOBJ|MF2_TELESTOMP;
	self->spawnstate = S_CHICPLAY;
	self->seestate = S_CHICPLAY_RUN1;
	self->missilestate = S_CHICPLAY_ATK1;
	self->painstate = S_CHICPLAY_PAIN;
	self->deathstate = S_CHICKEN_DIE1;
	self->sound_pain = GetSoundID("ChickenPain");
	self->sound_death = GetSoundID("ChickenDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_CHICKEN
//
//==========================================================================

void misc_CHICKEN(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 10;
	self->spawnhealth = self->health;
	self->radius = 9.0;
	self->height = 22.0;
	self->mass = 40.0;
	self->speed = 140.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 200;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_DROPOFF|MF_COUNTKILL;
	self->flags2 = MF2_WINDTHRUST|MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_CHICKEN_LOOK1;
	self->seestate = S_CHICKEN_WALK1;
	self->meleestate = S_CHICKEN_ATK1;
	self->painstate = S_CHICKEN_PAIN1;
	self->deathstate = S_CHICKEN_DIE1;
	self->sound_sight = GetSoundID("ChickenPain");
	self->sound_active = GetSoundID("ChickenActive");
	self->sound_attack = GetSoundID("ChickenAttack");
	self->sound_pain = GetSoundID("ChickenPain");
	self->sound_death = GetSoundID("ChickenDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_FEATHER
//
//==========================================================================

void misc_FEATHER(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 2.0;
	self->height = 4.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_LOGRAV|MF2_WINDTHRUST|MF2_NOTELEPORT|MF2_CANNOTPUSH;
	self->spawnstate = S_FEATHER1;
	self->deathstate = S_FEATHERX;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MUMMY
//
//==========================================================================

void misc_MUMMY(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 80;
	self->spawnhealth = self->health;
	self->radius = 22.0;
	self->height = 62.0;
	self->mass = 75.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 128;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_MUMMY_LOOK1;
	self->seestate = S_MUMMY_WALK1;
	self->meleestate = S_MUMMY_ATK1;
	self->painstate = S_MUMMY_PAIN1;
	self->deathstate = S_MUMMY_DIE1;
	self->sound_sight = GetSoundID("MumySight");
	self->sound_active = GetSoundID("MumyActive");
	self->sound_attack = GetSoundID("MumyAttack1");
	self->sound_pain = GetSoundID("MumyPain");
	self->sound_death = GetSoundID("MumyDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MUMMYLEADER
//
//==========================================================================

void misc_MUMMYLEADER(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 100;
	self->spawnhealth = self->health;
	self->radius = 22.0;
	self->height = 62.0;
	self->mass = 75.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 64;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_MUMMY_LOOK1;
	self->seestate = S_MUMMY_WALK1;
	self->meleestate = S_MUMMY_ATK1;
	self->missilestate = S_MUMMYL_ATK1;
	self->painstate = S_MUMMY_PAIN1;
	self->deathstate = S_MUMMY_DIE1;
	self->sound_sight = GetSoundID("MumySight");
	self->sound_active = GetSoundID("MumyActive");
	self->sound_attack = GetSoundID("MumyAttack1");
	self->sound_pain = GetSoundID("MumyPain");
	self->sound_death = GetSoundID("MumyDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MUMMYGHOST
//
//==========================================================================

void misc_MUMMYGHOST(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->translucency = 33;
	self->health = 80;
	self->spawnhealth = self->health;
	self->radius = 22.0;
	self->height = 62.0;
	self->mass = 75.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 128;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_MUMMY_LOOK1;
	self->seestate = S_MUMMY_WALK1;
	self->meleestate = S_MUMMY_ATK1;
	self->painstate = S_MUMMY_PAIN1;
	self->deathstate = S_MUMMY_DIE1;
	self->sound_sight = GetSoundID("MumySight");
	self->sound_active = GetSoundID("MumyActive");
	self->sound_attack = GetSoundID("MumyAttack1");
	self->sound_pain = GetSoundID("MumyPain");
	self->sound_death = GetSoundID("MumyDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MUMMYLEADERGHOST
//
//==========================================================================

void misc_MUMMYLEADERGHOST(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->translucency = 33;
	self->health = 100;
	self->spawnhealth = self->health;
	self->radius = 22.0;
	self->height = 62.0;
	self->mass = 75.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 64;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_MUMMY_LOOK1;
	self->seestate = S_MUMMY_WALK1;
	self->meleestate = S_MUMMY_ATK1;
	self->missilestate = S_MUMMYL_ATK1;
	self->painstate = S_MUMMY_PAIN1;
	self->deathstate = S_MUMMY_DIE1;
	self->sound_sight = GetSoundID("MumySight");
	self->sound_active = GetSoundID("MumyActive");
	self->sound_attack = GetSoundID("MumyAttack1");
	self->sound_pain = GetSoundID("MumyPain");
	self->sound_death = GetSoundID("MumyDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MUMMYSOUL
//
//==========================================================================

void misc_MUMMYSOUL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->spawnstate = S_MUMMY_SOUL1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MUMMYFX1
//
//==========================================================================

void misc_MUMMYFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 14.0;
	self->mass = 100.0;
	self->speed = 315.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 4;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_MUMMYFX1_1;
	self->deathstate = S_MUMMYFXI1_1;
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BEAST
//
//==========================================================================

void misc_BEAST(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 220;
	self->spawnhealth = self->health;
	self->gibshealth = -110;
	self->radius = 32.0;
	self->height = 74.0;
	self->mass = 200.0;
	self->speed = 490.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 100;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_BEAST_LOOK1;
	self->seestate = S_BEAST_WALK1;
	self->missilestate = S_BEAST_ATK1;
	self->painstate = S_BEAST_PAIN1;
	self->deathstate = S_BEAST_DIE1;
	self->xdeathstate = S_BEAST_XDIE1;
	self->sound_sight = GetSoundID("BeastSight");
	self->sound_active = GetSoundID("BeastActive");
	self->sound_attack = GetSoundID("BeastAttack");
	self->sound_pain = GetSoundID("BeastPain");
	self->sound_death = GetSoundID("BeastDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BEASTBALL
//
//==========================================================================

void misc_BEASTBALL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 9.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 4;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_WINDTHRUST|MF2_NOTELEPORT;
	self->spawnstate = S_BEASTBALL1;
	self->deathstate = S_BEASTBALLX1;
	self->effects = EF_DL_RED;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BURNBALL
//
//==========================================================================

void misc_BURNBALL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 6.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_BURNBALL1;
	self->deathstate = S_BEASTBALLX1;
	self->effects = EF_DL_RED;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BURNBALLFB
//
//==========================================================================

void misc_BURNBALLFB(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 6.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_BURNBALLFB1;
	self->deathstate = S_BEASTBALLX1;
	self->effects = EF_DL_RED;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_PUFFY
//
//==========================================================================

void misc_PUFFY(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 6.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_PUFFY1;
	self->deathstate = S_PUFFY1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SNAKE
//
//==========================================================================

void misc_SNAKE(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 280;
	self->spawnhealth = self->health;
	self->radius = 22.0;
	self->height = 70.0;
	self->mass = 100.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 48;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_SNAKE_LOOK1;
	self->seestate = S_SNAKE_WALK1;
	self->missilestate = S_SNAKE_ATK1;
	self->painstate = S_SNAKE_PAIN1;
	self->deathstate = S_SNAKE_DIE1;
	self->sound_sight = GetSoundID("SnakeSight");
	self->sound_active = GetSoundID("SnakeActive");
	self->sound_attack = GetSoundID("SnakeAttack");
	self->sound_pain = GetSoundID("SnakePain");
	self->sound_death = GetSoundID("SnakeDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SNAKEPRO_A
//
//==========================================================================

void misc_SNAKEPRO_A(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 490.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 1;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_WINDTHRUST|MF2_NOTELEPORT;
	self->spawnstate = S_SNAKEPRO_A1;
	self->deathstate = S_SNAKEPRO_AX1;
	self->effects = EF_DL_MAGENTA;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SNAKEPRO_B
//
//==========================================================================

void misc_SNAKEPRO_B(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 490.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 3;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_SNAKEPRO_B1;
	self->deathstate = S_SNAKEPRO_BX1;
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_HEAD
//
//==========================================================================

void misc_HEAD(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 700;
	self->spawnhealth = self->health;
	self->radius = 40.0;
	self->height = 72.0;
	self->mass = 325.0;
	self->speed = 210.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 32;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_NOBLOOD|MF_COUNTKILL;
	self->flags2 = MF2_PASSMOBJ;
	self->spawnstate = S_HEAD_LOOK;
	self->seestate = S_HEAD_FLOAT;
	self->missilestate = S_HEAD_ATK1;
	self->painstate = S_HEAD_PAIN1;
	self->deathstate = S_HEAD_DIE1;
	self->sound_sight = GetSoundID("HeadSight");
	self->sound_active = GetSoundID("HeadActive");
	self->sound_attack = GetSoundID("HeadAttack1");
	self->sound_pain = GetSoundID("HeadPain");
	self->sound_death = GetSoundID("HeadDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_HEADFX1
//
//==========================================================================

void misc_HEADFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 455.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 1;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_THRUGHOST|MF2_NOTELEPORT;
	self->spawnstate = S_HEADFX1_1;
	self->deathstate = S_HEADFXI1_1;
	self->effects = EF_DL_BLUE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_HEADFX2
//
//==========================================================================

void misc_HEADFX2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 12.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 280.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 3;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_HEADFX2_1;
	self->deathstate = S_HEADFXI2_1;
	self->effects = EF_DL_BLUE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_HEADFX3
//
//==========================================================================

void misc_HEADFX3(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 14.0;
	self->height = 12.0;
	self->mass = 100.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 5;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_WINDTHRUST|MF2_NOTELEPORT;
	self->spawnstate = S_HEADFX3_1;
	self->deathstate = S_HEADFXI3_1;
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_WHIRLWIND
//
//==========================================================================

void misc_WHIRLWIND(mobj_t *self, mthing_t *mthing)
{
	self->translucency = 33;
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 74.0;
	self->mass = 100.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 1;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_HEADFX4_1;
	self->deathstate = S_HEADFXI4_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_CLINK
//
//==========================================================================

void misc_CLINK(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 150;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 64.0;
	self->mass = 75.0;
	self->speed = 490.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 32;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_NOBLOOD|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_CLINK_LOOK1;
	self->seestate = S_CLINK_WALK1;
	self->meleestate = S_CLINK_ATK1;
	self->painstate = S_CLINK_PAIN1;
	self->deathstate = S_CLINK_DIE1;
	self->sound_sight = GetSoundID("ClinkSight");
	self->sound_active = GetSoundID("ClinkActive");
	self->sound_attack = GetSoundID("ClinkAttack");
	self->sound_pain = GetSoundID("ClinkPain");
	self->sound_death = GetSoundID("ClinkDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_WIZARD
//
//==========================================================================

void misc_WIZARD(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 180;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 68.0;
	self->mass = 100.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 64;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_NOGRAVITY|MF_FLOAT|MF_COUNTKILL;
	self->flags2 = MF2_PASSMOBJ;
	self->spawnstate = S_WIZARD_LOOK1;
	self->seestate = S_WIZARD_WALK1;
	self->missilestate = S_WIZARD_ATK1;
	self->painstate = S_WIZARD_PAIN1;
	self->deathstate = S_WIZARD_DIE1;
	self->sound_sight = GetSoundID("WizardSight");
	self->sound_active = GetSoundID("WizardActive");
	self->sound_attack = GetSoundID("WizardAttack");
	self->sound_pain = GetSoundID("WizardPain");
	self->sound_death = GetSoundID("WizardDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_WIZFX1
//
//==========================================================================

void misc_WIZFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 10.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 630.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 3;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_WIZFX1_1;
	self->deathstate = S_WIZFXI1_1;
	self->effects = EF_DL_MAGENTA;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_IMP
//
//==========================================================================

void misc_IMP(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 40;
	self->spawnhealth = self->health;
	self->gibshealth = -20;
	self->radius = 16.0;
	self->height = 36.0;
	self->mass = 50.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 200;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_NOGRAVITY|MF_FLOAT|MF_COUNTKILL;
	self->flags2 = MF2_SPAWNFLOAT|MF2_PASSMOBJ;
	self->spawnstate = S_IMP_LOOK1;
	self->seestate = S_IMP_FLY1;
	self->meleestate = S_IMP_MEATK1;
	self->missilestate = S_IMP_MSATK1_1;
	self->painstate = S_IMP_PAIN1;
	self->crashstate = S_IMP_CRASH1;
	self->deathstate = S_IMP_DIE1;
	self->xdeathstate = S_IMP_XDIE1;
	self->sound_sight = GetSoundID("ImpSight");
	self->sound_active = GetSoundID("ImpActive");
	self->sound_attack = GetSoundID("ImpAttack1");
	self->sound_pain = GetSoundID("ImpPain");
	self->sound_death = GetSoundID("ImpDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_IMPLEADER
//
//==========================================================================

void misc_IMPLEADER(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 80;
	self->spawnhealth = self->health;
	self->gibshealth = -40;
	self->radius = 16.0;
	self->height = 36.0;
	self->mass = 50.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 200;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_NOGRAVITY|MF_FLOAT|MF_COUNTKILL;
	self->flags2 = MF2_SPAWNFLOAT|MF2_PASSMOBJ;
	self->spawnstate = S_IMP_LOOK1;
	self->seestate = S_IMP_FLY1;
	self->missilestate = S_IMP_MSATK2_1;
	self->painstate = S_IMP_PAIN1;
	self->crashstate = S_IMP_CRASH1;
	self->deathstate = S_IMP_DIE1;
	self->xdeathstate = S_IMP_XDIE1;
	self->sound_sight = GetSoundID("ImpSight");
	self->sound_active = GetSoundID("ImpActive");
	self->sound_attack = GetSoundID("ImpAttack2");
	self->sound_pain = GetSoundID("ImpPain");
	self->sound_death = GetSoundID("ImpDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_IMPCHUNK1
//
//==========================================================================

void misc_IMPCHUNK1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP;
	self->spawnstate = S_IMP_CHUNKA1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_IMPCHUNK2
//
//==========================================================================

void misc_IMPCHUNK2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP;
	self->spawnstate = S_IMP_CHUNKB1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_IMPBALL
//
//==========================================================================

void misc_IMPBALL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 350.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 1;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_WINDTHRUST|MF2_NOTELEPORT;
	self->spawnstate = S_IMPFX1;
	self->deathstate = S_IMPFXI1;
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_KNIGHT
//
//==========================================================================

void misc_KNIGHT(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 200;
	self->spawnhealth = self->health;
	self->radius = 24.0;
	self->height = 78.0;
	self->mass = 150.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 100;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_KNIGHT_STND1;
	self->seestate = S_KNIGHT_WALK1;
	self->meleestate = S_KNIGHT_ATK1;
	self->missilestate = S_KNIGHT_ATK1;
	self->painstate = S_KNIGHT_PAIN1;
	self->deathstate = S_KNIGHT_DIE1;
	self->sound_sight = GetSoundID("KnightSight");
	self->sound_active = GetSoundID("KnightActive");
	self->sound_attack = GetSoundID("KnightAttack1");
	self->sound_pain = GetSoundID("KnightPain");
	self->sound_death = GetSoundID("KnightDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_KNIGHTGHOST
//
//==========================================================================

void misc_KNIGHTGHOST(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->translucency = 33;
	self->health = 200;
	self->spawnhealth = self->health;
	self->radius = 24.0;
	self->height = 78.0;
	self->mass = 150.0;
	self->speed = 420.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 100;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ;
	self->spawnstate = S_KNIGHT_STND1;
	self->seestate = S_KNIGHT_WALK1;
	self->meleestate = S_KNIGHT_ATK1;
	self->missilestate = S_KNIGHT_ATK1;
	self->painstate = S_KNIGHT_PAIN1;
	self->deathstate = S_KNIGHT_DIE1;
	self->sound_sight = GetSoundID("KnightSight");
	self->sound_active = GetSoundID("KnightActive");
	self->sound_attack = GetSoundID("KnightAttack1");
	self->sound_pain = GetSoundID("KnightPain");
	self->sound_death = GetSoundID("KnightDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_KNIGHTAXE
//
//==========================================================================

void misc_KNIGHTAXE(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 10.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 315.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 2;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_WINDTHRUST|MF2_THRUGHOST|MF2_NOTELEPORT;
	self->spawnstate = S_SPINAXE1;
	self->deathstate = S_SPINAXEX1;
	self->sound_death = GetSoundID("SkullRodHit");
	self->effects = EF_DL_GREEN;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_REDAXE
//
//==========================================================================

void misc_REDAXE(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 10.0;
	self->height = 8.0;
	self->mass = 100.0;
	self->speed = 315.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 7;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_THRUGHOST|MF2_NOTELEPORT;
	self->spawnstate = S_REDAXE1;
	self->deathstate = S_REDAXEX1;
	self->sound_death = GetSoundID("SkullRodHit");
	self->effects = EF_DL_RED;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SORCERER1
//
//==========================================================================

void misc_SORCERER1(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 2000;
	self->spawnhealth = self->health;
	self->radius = 28.0;
	self->height = 100.0;
	self->mass = 800.0;
	self->speed = 560.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 56;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ|MF2_BOSS;
	self->spawnstate = S_SRCR1_LOOK1;
	self->seestate = S_SRCR1_WALK1;
	self->missilestate = S_SRCR1_ATK1;
	self->painstate = S_SRCR1_PAIN1;
	self->deathstate = S_SRCR1_DIE1;
	self->sound_sight = GetSoundID("SorcererDemonSight");
	self->sound_active = GetSoundID("SorcererDemonActive");
	self->sound_attack = GetSoundID("SorcererDemonAttack");
	self->sound_pain = GetSoundID("SorcererDemonPain");
	self->sound_death = GetSoundID("SorcererDemonDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SRCRFX1
//
//==========================================================================

void misc_SRCRFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 10.0;
	self->height = 10.0;
	self->mass = 100.0;
	self->speed = 700.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 10;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT|MF2_FIREDAMAGE;
	self->spawnstate = S_SRCRFX1_1;
	self->deathstate = S_SRCRFXI1_1;
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SORCERER2
//
//==========================================================================

void misc_SORCERER2(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 3500;
	self->spawnhealth = self->health;
	self->radius = 16.0;
	self->height = 70.0;
	self->mass = 300.0;
	self->speed = 490.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 32;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_DROPOFF|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ|MF2_BOSS;
	self->spawnstate = S_SOR2_LOOK1;
	self->seestate = S_SOR2_WALK1;
	self->missilestate = S_SOR2_ATK1;
	self->painstate = S_SOR2_PAIN1;
	self->deathstate = S_SOR2_DIE1;
	self->sound_sight = GetSoundID("SorcererSight");
	self->sound_active = GetSoundID("SorcererActive");
	self->sound_attack = GetSoundID("SorcererAttack");
	self->sound_pain = GetSoundID("SorcererPain");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SOR2FX1
//
//==========================================================================

void misc_SOR2FX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 10.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 700.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 1;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_SOR2FX1_1;
	self->deathstate = S_SOR2FXI1_1;
	self->effects = EF_DL_BLUE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SOR2FXSPARK
//
//==========================================================================

void misc_SOR2FXSPARK(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY;
	self->flags2 = MF2_NOTELEPORT|MF2_CANNOTPUSH;
	self->spawnstate = S_SOR2FXSPARK1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SOR2FX2
//
//==========================================================================

void misc_SOR2FX2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 10.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 210.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 10;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT;
	self->spawnstate = S_SOR2FX2_1;
	self->deathstate = S_SOR2FXI2_1;
	self->effects = EF_DL_BLUE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SOR2TELEFADE
//
//==========================================================================

void misc_SOR2TELEFADE(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOBLOCKMAP;
	self->spawnstate = S_SOR2TELEFADE1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MINOTAUR
//
//==========================================================================

void misc_MINOTAUR(mobj_t *self, mthing_t *mthing)
{
	if (nomonsters)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 3000;
	self->spawnhealth = self->health;
	self->radius = 28.0;
	self->height = 100.0;
	self->mass = 800.0;
	self->speed = 560.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->painchance = 25;
	self->damage = 7;
	self->flags = MF_SOLID|MF_SHOOTABLE|MF_DROPOFF|MF_COUNTKILL;
	self->flags2 = MF2_FOOTCLIP|MF2_PASSMOBJ|MF2_BOSS;
	self->spawnstate = S_MNTR_LOOK1;
	self->seestate = S_MNTR_WALK1;
	self->meleestate = S_MNTR_ATK1_1;
	self->missilestate = S_MNTR_ATK2_1;
	self->painstate = S_MNTR_PAIN1;
	self->deathstate = S_MNTR_DIE1;
	self->sound_sight = GetSoundID("MinotaurSight");
	self->sound_active = GetSoundID("MinotaurActive");
	self->sound_attack = GetSoundID("MinotaurAttack1");
	self->sound_pain = GetSoundID("MinotaurPain");
	self->sound_death = GetSoundID("MinotaurDeath");
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MNTRFX1
//
//==========================================================================

void misc_MNTRFX1(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 10.0;
	self->height = 6.0;
	self->mass = 100.0;
	self->speed = 700.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 3;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT|MF2_FIREDAMAGE;
	self->spawnstate = S_MNTRFX1_1;
	self->deathstate = S_MNTRFXI1_1;
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MNTRFX2
//
//==========================================================================

void misc_MNTRFX2(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 5.0;
	self->height = 12.0;
	self->mass = 100.0;
	self->speed = 490.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 4;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT|MF2_FIREDAMAGE;
	self->spawnstate = S_MNTRFX2_1;
	self->deathstate = S_MNTRFXI2_1;
	self->sound_death = GetSoundID("PhoenixHit");
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_MNTRFX3
//
//==========================================================================

void misc_MNTRFX3(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 8.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->damage = 4;
	self->flags = MF_NOBLOCKMAP|MF_NOGRAVITY|MF_DROPOFF|MF_MISSILE;
	self->flags2 = MF2_NOTELEPORT|MF2_FIREDAMAGE;
	self->spawnstate = S_MNTRFX3_1;
	self->deathstate = S_MNTRFXI2_1;
	self->sound_death = GetSoundID("PhoenixHit");
	self->effects = EF_DL_WHITE;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AKYY
//
//==========================================================================

void misc_AKYY(mobj_t *self, mthing_t *mthing)
{
	if (deathmatch)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AKYY1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_BKYY
//
//==========================================================================

void misc_BKYY(mobj_t *self, mthing_t *mthing)
{
	if (deathmatch)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_BKYY1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_CKEY
//
//==========================================================================

void misc_CKEY(mobj_t *self, mthing_t *mthing)
{
	if (deathmatch)
	{
		RemoveMobjThinker(self);
		return;
	}
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_CKYY1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMGWNDWIMPY
//
//==========================================================================

void misc_AMGWNDWIMPY(mobj_t *self, mthing_t *mthing)
{
	self->health = 10;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMG1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMGWNDHEFTY
//
//==========================================================================

void misc_AMGWNDHEFTY(mobj_t *self, mthing_t *mthing)
{
	self->health = 50;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMG2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMMACEWIMPY
//
//==========================================================================

void misc_AMMACEWIMPY(mobj_t *self, mthing_t *mthing)
{
	self->health = 20;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMM1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMMACEHEFTY
//
//==========================================================================

void misc_AMMACEHEFTY(mobj_t *self, mthing_t *mthing)
{
	self->health = 100;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMM2;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMCBOWWIMPY
//
//==========================================================================

void misc_AMCBOWWIMPY(mobj_t *self, mthing_t *mthing)
{
	self->health = 5;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMC1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMCBOWHEFTY
//
//==========================================================================

void misc_AMCBOWHEFTY(mobj_t *self, mthing_t *mthing)
{
	self->health = 20;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMC2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMSKRDWIMPY
//
//==========================================================================

void misc_AMSKRDWIMPY(mobj_t *self, mthing_t *mthing)
{
	self->health = 20;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMS1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMSKRDHEFTY
//
//==========================================================================

void misc_AMSKRDHEFTY(mobj_t *self, mthing_t *mthing)
{
	self->health = 100;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMS2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMPHRDWIMPY
//
//==========================================================================

void misc_AMPHRDWIMPY(mobj_t *self, mthing_t *mthing)
{
	self->health = 1;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMP1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMPHRDHEFTY
//
//==========================================================================

void misc_AMPHRDHEFTY(mobj_t *self, mthing_t *mthing)
{
	self->health = 10;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMP2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMBLSRWIMPY
//
//==========================================================================

void misc_AMBLSRWIMPY(mobj_t *self, mthing_t *mthing)
{
	self->health = 10;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMB1_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_AMBLSRHEFTY
//
//==========================================================================

void misc_AMBLSRHEFTY(mobj_t *self, mthing_t *mthing)
{
	self->health = 25;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_SPECIAL;
	self->spawnstate = S_AMB2_1;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SOUNDWIND
//
//==========================================================================

void misc_SOUNDWIND(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOSECTOR|MF_NOBLOCKMAP;
	self->spawnstate = S_SND_WIND;
	generic_mobj_start(self, mthing);
}

//==========================================================================
//
//	misc_SOUNDWATERFALL
//
//==========================================================================

void misc_SOUNDWATERFALL(mobj_t *self, mthing_t *mthing)
{
	self->health = 1000;
	self->spawnhealth = self->health;
	self->radius = 20.0;
	self->height = 16.0;
	self->mass = 100.0;
	if (gameskill != sk_nightmare)
		self->reactiontime = 8;
	self->flags = MF_NOSECTOR|MF_NOBLOCKMAP;
	self->spawnstate = S_SND_WATERFALL;
	generic_mobj_start(self, mthing);
}

__mobjinfo__
{
	MT_MISC0 { 81, misc_MISC0 }
	MT_ITEMSHIELD1 { 85, misc_ITEMSHIELD1 }
	MT_ITEMSHIELD2 { 31, misc_ITEMSHIELD2 }
	MT_MISC1 { 8, misc_MISC1 }
	MT_MISC2 { 35, misc_MISC2 }
	MT_ARTIINVISIBILITY { 75, misc_ARTIINVISIBILITY }
	MT_MISC3 { 82, misc_MISC3 }
	MT_ARTIFLY { 83, misc_ARTIFLY }
	MT_ARTIINVULNERABILITY { 84, misc_ARTIINVULNERABILITY }
	MT_ARTITOMEOFPOWER { 86, misc_ARTITOMEOFPOWER }
	MT_ARTIEGG { 30, misc_ARTIEGG }
	MT_EGGFX { -1, misc_EGGFX }
	MT_ARTISUPERHEAL { 32, misc_ARTISUPERHEAL }
	MT_MISC4 { 33, misc_MISC4 }
	MT_MISC5 { 34, misc_MISC5 }
	MT_FIREBOMB { -1, misc_FIREBOMB }
	MT_ARTITELEPORT { 36, misc_ARTITELEPORT }
	MT_POD { 2035, misc_POD }
	MT_PODGOO { -1, misc_PODGOO }
	MT_PODGENERATOR { 43, misc_PODGENERATOR }
	MT_SPLASH { -1, misc_SPLASH }
	MT_SPLASHBASE { -1, misc_SPLASHBASE }
	MT_LAVASPLASH { -1, misc_LAVASPLASH }
	MT_LAVASMOKE { -1, misc_LAVASMOKE }
	MT_SLUDGECHUNK { -1, misc_SLUDGECHUNK }
	MT_SLUDGESPLASH { -1, misc_SLUDGESPLASH }
	MT_SKULLHANG70 { 17, misc_SKULLHANG70 }
	MT_SKULLHANG60 { 24, misc_SKULLHANG60 }
	MT_SKULLHANG45 { 25, misc_SKULLHANG45 }
	MT_SKULLHANG35 { 26, misc_SKULLHANG35 }
	MT_CHANDELIER { 28, misc_CHANDELIER }
	MT_SERPTORCH { 27, misc_SERPTORCH }
	MT_SMALLPILLAR { 29, misc_SMALLPILLAR }
	MT_STALAGMITESMALL { 37, misc_STALAGMITESMALL }
	MT_STALAGMITELARGE { 38, misc_STALAGMITELARGE }
	MT_STALACTITESMALL { 39, misc_STALACTITESMALL }
	MT_STALACTITELARGE { 40, misc_STALACTITELARGE }
	MT_MISC6 { 76, misc_MISC6 }
	MT_BARREL { 44, misc_BARREL }
	MT_MISC7 { 47, misc_MISC7 }
	MT_MISC8 { 48, misc_MISC8 }
	MT_MISC9 { 49, misc_MISC9 }
	MT_MISC10 { 50, misc_MISC10 }
	MT_MISC11 { 51, misc_MISC11 }
	MT_KEYGIZMOBLUE { 94, misc_KEYGIZMOBLUE }
	MT_KEYGIZMOGREEN { 95, misc_KEYGIZMOGREEN }
	MT_KEYGIZMOYELLOW { 96, misc_KEYGIZMOYELLOW }
	MT_KEYGIZMOFLOAT { -1, misc_KEYGIZMOFLOAT }
	MT_MISC12 { 87, misc_MISC12 }
	MT_VOLCANOBLAST { -1, misc_VOLCANOBLAST }
	MT_VOLCANOTBLAST { -1, misc_VOLCANOTBLAST }
	MT_TELEGLITGEN { 74, misc_TELEGLITGEN }
	MT_TELEGLITGEN2 { 52, misc_TELEGLITGEN2 }
	MT_TELEGLITTER { -1, misc_TELEGLITTER }
	MT_TELEGLITTER2 { -1, misc_TELEGLITTER2 }
	MT_TFOG { -1, misc_TFOG }
	MT_TELEPORTMAN { 14, misc_TELEPORTMAN }
	MT_STAFFPUFF { -1, misc_STAFFPUFF }
	MT_STAFFPUFF2 { -1, misc_STAFFPUFF2 }
	MT_BEAKPUFF { -1, misc_BEAKPUFF }
	MT_MISC13 { 2005, misc_MISC13 }
	MT_GAUNTLETPUFF1 { -1, misc_GAUNTLETPUFF1 }
	MT_GAUNTLETPUFF2 { -1, misc_GAUNTLETPUFF2 }
	MT_MISC14 { 53, misc_MISC14 }
	MT_BLASTERFX1 { -1, misc_BLASTERFX1 }
	MT_BLASTERSMOKE { -1, misc_BLASTERSMOKE }
	MT_RIPPER { -1, misc_RIPPER }
	MT_BLASTERPUFF1 { -1, misc_BLASTERPUFF1 }
	MT_BLASTERPUFF2 { -1, misc_BLASTERPUFF2 }
	MT_WMACE { 2002, misc_WMACE }
	MT_MACEFX1 { -1, misc_MACEFX1 }
	MT_MACEFX2 { -1, misc_MACEFX2 }
	MT_MACEFX3 { -1, misc_MACEFX3 }
	MT_MACEFX4 { -1, misc_MACEFX4 }
	MT_WSKULLROD { 2004, misc_WSKULLROD }
	MT_HORNRODFX1 { -1, misc_HORNRODFX1 }
	MT_HORNRODFX2 { -1, misc_HORNRODFX2 }
	MT_RAINPLR1 { -1, misc_RAINPLR1 }
	MT_RAINPLR2 { -1, misc_RAINPLR2 }
	MT_RAINPLR3 { -1, misc_RAINPLR3 }
	MT_RAINPLR4 { -1, misc_RAINPLR4 }
	MT_GOLDWANDFX1 { -1, misc_GOLDWANDFX1 }
	MT_GOLDWANDFX2 { -1, misc_GOLDWANDFX2 }
	MT_GOLDWANDPUFF1 { -1, misc_GOLDWANDPUFF1 }
	MT_GOLDWANDPUFF2 { -1, misc_GOLDWANDPUFF2 }
	MT_WPHOENIXROD { 2003, misc_WPHOENIXROD }
	MT_PHOENIXFX1 { -1, misc_PHOENIXFX1 }
	MT_PHOENIXPUFF { -1, misc_PHOENIXPUFF }
	MT_PHOENIXFX2 { -1, misc_PHOENIXFX2 }
	MT_MISC15 { 2001, misc_MISC15 }
	MT_CRBOWFX1 { -1, misc_CRBOWFX1 }
	MT_CRBOWFX2 { -1, misc_CRBOWFX2 }
	MT_CRBOWFX3 { -1, misc_CRBOWFX3 }
	MT_CRBOWFX4 { -1, misc_CRBOWFX4 }
	MT_BLOOD { -1, misc_BLOOD }
	MT_BLOODSPLATTER { -1, misc_BLOODSPLATTER }
	MT_PLAYER { -1, misc_PLAYER }
	MT_BLOODYSKULL { -1, misc_BLOODYSKULL }
	MT_CHICPLAYER { -1, misc_CHICPLAYER }
	MT_CHICKEN { -1, misc_CHICKEN }
	MT_FEATHER { -1, misc_FEATHER }
	MT_MUMMY { 68, misc_MUMMY }
	MT_MUMMYLEADER { 45, misc_MUMMYLEADER }
	MT_MUMMYGHOST { 69, misc_MUMMYGHOST }
	MT_MUMMYLEADERGHOST { 46, misc_MUMMYLEADERGHOST }
	MT_MUMMYSOUL { -1, misc_MUMMYSOUL }
	MT_MUMMYFX1 { -1, misc_MUMMYFX1 }
	MT_BEAST { 70, misc_BEAST }
	MT_BEASTBALL { -1, misc_BEASTBALL }
	MT_BURNBALL { -1, misc_BURNBALL }
	MT_BURNBALLFB { -1, misc_BURNBALLFB }
	MT_PUFFY { -1, misc_PUFFY }
	MT_SNAKE { 92, misc_SNAKE }
	MT_SNAKEPRO_A { -1, misc_SNAKEPRO_A }
	MT_SNAKEPRO_B { -1, misc_SNAKEPRO_B }
	MT_HEAD { 6, misc_HEAD }
	MT_HEADFX1 { -1, misc_HEADFX1 }
	MT_HEADFX2 { -1, misc_HEADFX2 }
	MT_HEADFX3 { -1, misc_HEADFX3 }
	MT_WHIRLWIND { -1, misc_WHIRLWIND }
	MT_CLINK { 90, misc_CLINK }
	MT_WIZARD { 15, misc_WIZARD }
	MT_WIZFX1 { -1, misc_WIZFX1 }
	MT_IMP { 66, misc_IMP }
	MT_IMPLEADER { 5, misc_IMPLEADER }
	MT_IMPCHUNK1 { -1, misc_IMPCHUNK1 }
	MT_IMPCHUNK2 { -1, misc_IMPCHUNK2 }
	MT_IMPBALL { -1, misc_IMPBALL }
	MT_KNIGHT { 64, misc_KNIGHT }
	MT_KNIGHTGHOST { 65, misc_KNIGHTGHOST }
	MT_KNIGHTAXE { -1, misc_KNIGHTAXE }
	MT_REDAXE { -1, misc_REDAXE }
	MT_SORCERER1 { 7, misc_SORCERER1 }
	MT_SRCRFX1 { -1, misc_SRCRFX1 }
	MT_SORCERER2 { -1, misc_SORCERER2 }
	MT_SOR2FX1 { -1, misc_SOR2FX1 }
	MT_SOR2FXSPARK { -1, misc_SOR2FXSPARK }
	MT_SOR2FX2 { -1, misc_SOR2FX2 }
	MT_SOR2TELEFADE { -1, misc_SOR2TELEFADE }
	MT_MINOTAUR { 9, misc_MINOTAUR }
	MT_MNTRFX1 { -1, misc_MNTRFX1 }
	MT_MNTRFX2 { -1, misc_MNTRFX2 }
	MT_MNTRFX3 { -1, misc_MNTRFX3 }
	MT_AKYY { 73, misc_AKYY }
	MT_BKYY { 79, misc_BKYY }
	MT_CKEY { 80, misc_CKEY }
	MT_AMGWNDWIMPY { 10, misc_AMGWNDWIMPY }
	MT_AMGWNDHEFTY { 12, misc_AMGWNDHEFTY }
	MT_AMMACEWIMPY { 13, misc_AMMACEWIMPY }
	MT_AMMACEHEFTY { 16, misc_AMMACEHEFTY }
	MT_AMCBOWWIMPY { 18, misc_AMCBOWWIMPY }
	MT_AMCBOWHEFTY { 19, misc_AMCBOWHEFTY }
	MT_AMSKRDWIMPY { 20, misc_AMSKRDWIMPY }
	MT_AMSKRDHEFTY { 21, misc_AMSKRDHEFTY }
	MT_AMPHRDWIMPY { 22, misc_AMPHRDWIMPY }
	MT_AMPHRDHEFTY { 23, misc_AMPHRDHEFTY }
	MT_AMBLSRWIMPY { 54, misc_AMBLSRWIMPY }
	MT_AMBLSRHEFTY { 55, misc_AMBLSRHEFTY }
	MT_SOUNDWIND { 42, misc_SOUNDWIND }
	MT_SOUNDWATERFALL { 41, misc_SOUNDWATERFALL }
}

//**************************************************************************
//
//	$Log$
//	Revision 1.3  2001/09/20 16:19:59  dj_jl
//	Start using object-oriented stuff, beautification
//
//	Revision 1.2  2001/07/27 14:27:52  dj_jl
//	Update with Id-s and Log-s, some fixes
//
//**************************************************************************
